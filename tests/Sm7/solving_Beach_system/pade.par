# Parameter file for Pade_approximants program.
-1 1 500   # Real energy mesh: wmin, wmax, Nw.
0.01       # Distance above the real axis.
-3 -1 1    # Lowest index in the input file to use: nminstart, nminfinish, nminstep. nmin < 0 mirror values are added.
50 100 4   # Number of input points to use: Mmin, Mmax, Mstep.
40 80 4    # Number of Pade approximant coefficients to use: Nmin, Nmax, Nstep.
.false.    # To use the only diagonal configurations where M == N.
0          # Parameter determining how to evaluate the Pade approximant at the output points.
.false.    # Calculate poles and residues for all the Pade approximants.
64        # Numerical precision use in the calculation of the Pade approximants. 64 for double, 128 for quadruple.
0          # If (0) 1, (no) multiplication with transpose matrix. 2 means explicit SVD.
0.99 0.51  # Criteria values for the averaging. 
